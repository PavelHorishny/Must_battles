package org.game.services;

import org.game.map.Surface;
import org.game.map.SurfaceType;
import org.game.mockData.MockedData;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Test;

import java.util.Arrays;

import static org.junit.jupiter.api.Assertions.*;

class MapProcessorTest {
/*    static Surface [] [] map;
    static MapProcessor mapProcessor;
    @BeforeAll
    static void start(){
        mapProcessor = new MapProcessor();
        map = mapProcessor.generateStandardMap();
    }
    @Test
    void checkPortLocationMethod(){
        MockedData.STANDARD_FORT_POSITION.forEach(fortification -> {
            mapProcessor.setPortLocations(map,fortification);
            fortification.getPort().forEach(port->assertEquals(port.getType(), SurfaceType.PORT));
        });

    }*/

}